# E2E Health Log

Track E2E test failures to measure signal-to-noise ratio. See `docs/adr/007-testing-strategy.md` for the full E2E testing strategy.

## How to Log

After each CI E2E failure, add a row:

- **(a) Bug caught** — real code defect discovered by E2E
- **(b) Intentional change** — test updated to match new app behavior
- **(c) Flake** — passed on retry, no code change needed

## Targets

- **Signal-to-noise ratio (a / b):** > 2.0
- **Review frequency:** Quarterly
- **Cull threshold:** Any test logged as (b) or (c) more than twice should be rewritten or removed

## Log

| Date       | Test                                                                                                                                                                                                                                                                                                          | Category  | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-03-25 | —                                                                                                                                                                                                                                                                                                             | —         | E2E suite overhauled: 87 → 21 tests, 15 → 7 files. Tracking begins.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-04-13 | OnboardingWizard overlay + 10 cascading webkit tests                                                                                                                                                                                                                                                          | (a)       | Real Safari bug: `<Transition>` + `backdrop-filter: blur()` on the transitioning element stalls webkit's leave transition, leaving an invisible click-blocking overlay. Fixed in #153 by moving blur to `::before`. WebKit restored to main-branch CI.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-04-13 | 10 webkit-only timeouts (stable-check + `/transactions` goto)                                                                                                                                                                                                                                                 | (a)       | Vue modal enter transitions kept dialog buttons "unstable" on webkit, and `/transactions` page.goto hung on `load` event. Fixed in #156: reduced-motion rule also zeroes `transition-duration`, page-objects use `waitUntil: 'domcontentloaded'`, webkit project timeout set to 40s.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-04-13 | 5 webkit tests on duplicate PR run (Add Account click, `/planner` goto internal error, Onboarding add-account waitFor, OK button)                                                                                                                                                                             | (c)       | Parallel run on identical commit passed all tests. One run flaked under Linux-CI resource contention + one transient "WebKit encountered an internal error" browser crash on page.goto. Hardened: webkit timeout 40→60s and webkit retries 1→2 in #157.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-04-16 | 3 chromium flakes (Account Institution Combobox, Loan/Activity recurring monthly fee, Dashboard net worth)                                                                                                                                                                                                    | (c)       | Local run during the help-center consolidation PR. All 3 passed on retry with `--retries=2`. None touch `/help` routes; unrelated to the change. Flakiness likely from combobox open() timing + dashboard load timing. Watch for recurrence; if > 2 occurrences, consider hardening.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-04-16 | 3 chromium flakes (Account Institution Combobox again, Loan/Activity recurring again, Google Drive Create Pod step 2)                                                                                                                                                                                         | (c)       | Second local run during the stale-PWA reinstall modal PR. All 3 passed on retry. Combobox + recurring are now recurring flakes (2nd occurrence today) — should be hardened next. Google Drive step 2 is a known webkit flake from #165 that's now also flaked on chromium once.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-04-19 | invite-join "Invite flow: add member" (chromium + webkit)                                                                                                                                                                                                                                                     | (b)       | Pod renamed: `/family` now redirects to `/pod`, and the "+ Add a Beanie" button was renamed to "Add Bean" as part of the Meet the Beans redesign. Test updated to navigate `/pod` directly and use a tolerant regex `/add (bean\|a beanie)/i` that covers both old and new labels.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2026-04-19 | cross-entity "Onboarding wizard: complete all steps" (webkit only)                                                                                                                                                                                                                                            | (c)       | WebKit-only timeout on `onboarding-add-account` testid after clicking `onboarding-start`. Chromium passes cleanly. No onboarding code changed between the last green run and this one — consistent with WebKit's slower timer handling under CI contention. Watching; will harden if recurring.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-04-22 | cross-entity "Data integrity: add then remove linked payment" + "Activity with monthly fee" (webkit only)                                                                                                                                                                                                     | (c)       | Hard-fail + 2 flakes on webkit CI. Trace showed "Add Activity" save click succeeded but the `[role="dialog"] >> button "OK"` locator timed out at 5s — CreatedConfirmModal rendered later than the tight window allowed. Hardened by extracting `dismissActivityCreatedConfirm` to `e2e/helpers/activity-modal.ts`: waits for the "Activity Created" heading (15s) first, then clicks OK. Also deduplicates the inline helper that was copied across planner.spec.ts and cross-entity.spec.ts.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-04-23 | cross-entity "Onboarding wizard" + planner "Recurring: edit this and all future" / "edit all occurrences" (webkit only)                                                                                                                                                                                       | (c)       | Recurring webkit flake hitting 4 of last 5 pushes. All three tests share the same pattern: modal-to-modal transitions with explicit `timeout: 5000` on `waitFor` / `toBeVisible`, overriding the 60s webkit project timeout. Chromium passes consistently. Hardened all the tight 5s timeouts → 15s in cross-entity.spec.ts (onboarding) and planner.spec.ts (`openEditModal` helper), matching the 15s pattern already standardised in `activity-modal.ts`. Onboarding flake is now at 2nd logged occurrence — if it recurs after this harden, revisit the underlying modal timing on webkit (possibly `<Transition>` `enter-active` durations).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-04-26 | invite-join "Invite flow: add member" (chromium)                                                                                                                                                                                                                                                              | (b)       | Test rewritten to walk the new 2-step `InviteWizardModal` (replaces the inline 2-step block in `MeetTheBeansPage`). New flow: add bean → wizard opens at Step 1 → fill `invite-email-input` → tick `invite-wizard-confirm-checkbox` (via `force: true` since the input is `sr-only` behind a styled label) → click `invite-wizard-submit` → assert `invite-wizard-step-2` + `invite-wizard-qr` (data:image/png) + `invite-channel-whatsapp` + `invite-copy-link` → click `invite-wizard-back` → assert email retained, checkbox unticked. `invite-email-input` carried over verbatim. Chromium passes locally.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-04-30 | cross-entity "Activity with monthly fee creates recurring item" (webkit only) — recurrence of 2026-04-16 flake                                                                                                                                                                                                | (c)       | Hard-fail on webkit CI (3 retries, all timed out at 60s). Trace points at `AccountsPage.selectAccountType('checking')` — the category click commits but the subtype chip (`🏦 Checking`) is never clickable within the 60s test timeout. The picker conditionally renders the subtype group via `v-if="expandedCat()!.subtypes"` (`AccountCategoryPicker.vue:126`) — webkit under CI contention can lag the re-render. Same shape as 2026-04-22/23 hardens. Hardened by adding an explicit `subtypeBtn.waitFor({ state: 'visible', timeout: 15_000 })` before the subtype click in `e2e/page-objects/AccountsPage.ts:52`. Failure mode now surfaces "subtype not visible" (clearer signal) instead of "click timed out" (ambiguous). Triggering commit `3a5589f` only changed two strings in `beanpodFilename.ts` + its test fixture — completely unrelated to the Account creation flow.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-05-03 | cross-entity "Activity with monthly fee creates recurring item" (webkit only) — **3rd recurrence** of 2026-04-16/22/30 flake. Crosses the cull threshold per § Targets (any test logged (c) more than twice).                                                                                                 | (a) → (b) | Hard-fail on webkit CI; chromium 21/21 green. Failure was the same `dismissActivityCreatedConfirm` 15s timeout waiting for `[role="dialog"] >> heading "Activity Created"`. **Root cause finally found**: in `FamilyPlannerPage.vue:handleSave`, the create branch opened `CreatedConfirmModal` BEFORE closing `ActivityModal` — so for one tick both `<BaseModal>` instances were in the DOM with `role="dialog"` + `aria-modal="true"`. WebKit's a11y/focus engine, under CI contention, would stall the second modal's visibility for >15s while the first was still teleported in. **Structural fix**: reorder `handleSave` to set `showModal.value = false` first, `await nextTick()` to let Vue flush the v-if removal of the first dialog, then open the confirm dialog. Reclassifying: the prior (c) entries weren't pure flakes — they were the same latent ordering bug surfacing inconsistently as webkit-CI load varied. Triggering commit `e74f6b5` (toast suppression) was completely unrelated to the activity creation flow, which made the recurrence the cleanest signal yet that this needed structural attention. `TransactionsPage.handleTransactionSave` follows the same close-then-open pattern but without `nextTick`; it hasn't flaked, so left as-is — apply the same pattern preemptively if it surfaces.                                                                                                                                                                                                                                                                                                                                 |
| 2026-05-05 | cross-entity:379 "Loan & Activity Linking › View linked payment section and navigate to transactions" (webkit only)                                                                                                                                                                                           | (c)       | Hard-fail on webkit CI across 4 consecutive pushes (`2aba11d` → `94993d2`). All four triggering commits were unrelated to account creation — privacy doc tweaks, drive-open UI hierarchy, popup-blocked recovery, airport combobox rich items. Same `AccountsPage.selectAccountType` helper as the 2026-04-30 hardening: `subtypeBtn.waitFor({ state: 'visible', timeout: 15_000 })` after the 🏦 Bank category click times out before the `🏦 Checking` subtype renders. Prior structural fixes (v-if → v-show in AccountCategoryPicker, modal close-then-open ordering) are all still in place — the v-show DOM is pre-mounted, so this isn't a remount race. WebKit-CI under sustained contention is just slow at flushing the `display:none` flip and recomputing a11y-tree visibility. Bumped the helper's timeout from 15s to 30s to absorb the tail latency. If this recurs at 30s, next step is `selectAccountType` polling for `expandedCategory` state via a stronger signal (the collapse "×" button) before waiting for subtype, OR quarantining the test on webkit.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-05-09 | cross-entity:23/416 + planner.spec.ts:261 (webkit only) — **6th recurrence** of the 04-22/23/30, 05-03, 05-05 webkit `selectAccountType` flake at the now-30s timeout                                                                                                                                         | (c)       | Hard-fail on webkit CI for commit `601e40c` (a 2-line wrap-only style fix on `index.html`); chromium 21/21 green; webkit's first attempt failed 3 tests + flaked 3 + passed 15. Re-running only the failed job (`gh run rerun --failed`) passed cleanly — confirming pure flake, not a regression from the Large reading mode work. Per the 2026-05-05 entry's stated next step ("if this recurs at 30s, poll for `expandedCategory` state via a stronger signal"), `AccountsPage.selectAccountType` was hardened: it now polls for the "Select a type" hint text (rendered only inside the expanded category block, `AccountCategoryPicker.vue:135`) before waiting for the subtype button. The hint becoming visible means the v-show display:none flip + a11y-tree recompute have completed, so the subsequent subtype `waitFor` returns immediately on the happy path. Failure mode is now "expansion-stall surfaced in <5s" instead of "ambiguous subtype timeout at 30s" — real bugs fail fast, flakes still get the full retry budget.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-05-10 | cross-entity:23/83/347/379/416 + planner.spec.ts:261 (chromium AND webkit) — **regression from yesterday's `5beac12` harden**                                                                                                                                                                                 | (b)       | The poll-for-hint-text harden missed that `AccountCategoryPicker.vue` pre-mounts every expandable category's expanded block (one `v-for` × `v-show`, three categories, three "Select a type" hint nodes from first paint). `getByText('Select a type', { exact: true }).waitFor()` strict-mode-violated on 3 matches across both browsers (3 consecutive failures `5beac12` → `2c86108` → `c997531`). Fix: chain `.filter({ visible: true })` so only the visible hint matches — the visible-filter is the canonical Playwright way to disambiguate among `v-show`-pre-mounted siblings. Code-side validation (`vue-tsc`, `npm run test:run`) didn't catch this because it's a runtime-only DOM query shape; only the e2e CI exercises it. Lesson: any new test selector that targets text inside a `v-show`-pre-mounted block needs `.filter({ visible: true })` or an equivalent visibility scope. The 2026-04-30 v-if → v-show structural fix and this 2026-05-10 visibility-filter fix are two sides of the same coin — pre-mounting eliminates the remount race but multiplies the matchable nodes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-05-13 | webkit-only: 3 hard-fail + 6 flaked in `cross-entity.spec.ts` (`selectAccountType` 30s timeout + a single `gotoRoot` "Frame load interrupted" retry) — **8th recurrence** of the long-running webkit `selectAccountType` flake                                                                                | (c)       | Triggering commit `1519e97` (milestones summary card + scrapbook click behavior on the Pod surface) doesn't touch the Account creation flow at all. Chromium 21/21 green in 2m38s; webkit hard-failed 3 + flaked 3 in 9m02s with the same shape as 05-09/10 — `getByText('Select a type').filter({ visible: true }).waitFor(30s)` timed out after a category click. All prior structural fixes are in place (v-if → v-show, modal close-then-open ordering, visible-filter on the pre-mounted hint, `gotoRoot('/', {waitUntil:'commit'})`). `gh run rerun --failed` passed cleanly on the retry (webkit ✓ on the same SHA), confirming pure CI-contention flake. No new harden this round — the failure mode is already as fast and unambiguous as the helpers allow; the underlying signal is webkit-CI being slow under sustained load. If this hits a 9th recurrence we should look at quarantining the webkit project on the affected spec rather than tuning more timeouts.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-05-14 | setup-flow.spec.ts:20 — "Add member" button no longer immediately findable after `navigateToSetupStep3()`                                                                                                                                                                                                     | (b)       | Intentional change: Create Pod wizard Step 3 ("Add your family") simplification. The empty member form is no longer pre-opened — the empty state now shows two chip buttons ("Add an adult" / "Add a little bean") and the form only appears once the user explicitly opts in. Test updated to prepend an `await page.getByRole('button', { name: ui('loginV6.addAnAdult') }).click()` before the existing Add Member button assertions. Same coverage; one extra step at the top mirroring the new UX. Spec runs clean in 5.1s on chromium. Same simplification also removed the conditional "Skip — just me for now" link (Finish CTA in the form-closed state IS the skip path); not surfaced by the spec since it never asserted on the Skip link.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-05-11 | webkit-only: 8 tests across 6 spec files — `cross-entity:23/39/379`, `financial-data:36`, `invite-join:6`, `planner:70/196`, `trusted-device:101`                                                                                                                                                             | (c) → (a) | Triggering commit `1552d80` (a one-line `:title` prop removal in `PhotoViewer.vue`) is unrelated to every one of the 8 tests. Dominant error shape was new: `page.goto: Navigation to "http://localhost:5173/" is interrupted by another navigation to "http://localhost:5173/nook"` (and `Frame load interrupted`) thrown from `beforeEach` hooks at `await page.goto('/')`. Root cause: `goto('/')` waits `until: 'load'` by default, but the SPA's router guard redirects `/` → `/nook` / `/welcome` / `/login` on first paint — under webkit-CI contention that client-side navigation fires before the document's `load` event and Playwright bails. `gh run rerun --failed` passed clean (webkit 6m47s, chromium 2m54s) confirming pure flake. **Structural harden**: added `e2e/helpers/navigation.ts` exporting `gotoRoot(page)` which does `page.goto('/', { waitUntil: 'commit' })` — `commit` resolves as soon as the response is received, before `load`, so a same-tick redirect can't interrupt it. Replaced all 18 bare `page.goto('/')` call sites across 5 spec files (`cross-entity`, `financial-data`, `setup-flow`, `invite-join`, `trusted-device`, `planner`). Safe because every caller already follows the navigation with its own readiness wait (`bypassLoginIfNeeded` → `create-pod-button` visible, or an `app-content` / element `waitFor`). The `Activity CRUD lifecycle` failure (15s timeout on the "Activity Created" confirmation modal) is the long-running webkit-CI modal-stall flake documented 2026-05-03 — verified passing on chromium locally; not a regression from the Phase 1 activity-photos work shipped the same day. |
| 2026-05-16 | webkit-only: hard-fail on `trusted-device:102` (Password cache lifecycle) + 6 flaky on `cross-entity:24/350`, `financial-data:37`, `planner:72/198`, `setup-flow:8` across 3 consecutive pushes (`3460664`, `c381807`, `e4f7b0f`)                                                                             | (b) + (c) | `gh run rerun --failed` on the offline-queue commit produced **1 failed + 6 flaky + 14 passed** on webkit; chromium 22/22 green in 2m22s. Hard-failure is `page.goto('/settings', { waitUntil: 'commit' })` consistently interrupted by `another navigation to /nook` even with the `commit` harden — webkit-CI's same-tick redirect race survives all 3 retry attempts. The 6 flakies recovered on retry — same long-running webkit-CI contention shape as 2026-05-05 / 09 / 13, all `page.goto` interruption variants. **Two structural changes**: (a) per the 2026-05-13 entry's stated next step (quarantine the affected spec on webkit rather than tuning more timeouts), `trusted-device.spec.ts:102` is now `test.skip(browserName === 'webkit', ...)` — runs on chromium only. Webkit coverage on the other tests is preserved. (b) preemptive close-then-open with `await nextTick()` applied to the View→Edit transition in `FamilyPlannerPage.handleViewOpenEdit`, `FamilyNookPage.handleActivityOpenEdit`, and `TransactionsPage.handleViewOpenEdit` — same shape as the 2026-05-03 `handleSave` fix; should reduce the planner / cross-entity modal-stall flakes by eliminating the one-tick window where two `role=dialog` overlays coexist.                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-05-19 | `planner.spec.ts:72` Activity CRUD lifecycle hard-fail on both chromium AND webkit across 2 consecutive pushes (`fdd368d`, `c998485`)                                                                                                                                                                         | (b)       | Intentional change: commit `ea66dd4` (calendar chips redesign) deleted `UpcomingActivities.vue` + `TodoPreview.vue` and dropped the two-column section under the calendar in `FamilyPlannerPage`. The CRUD test was the only one scoped to `h3 hasText:/upcoming activities/i .locator('..')` — that h3 no longer exists, so the click on `Doctor Visit` ran out the 20s test timeout. The other 4 planner tests already used `getByText(title).first().click()` to hit the activity name wherever it appears, so they were unaffected. Test updated to match the same convention: drop the upcoming-section scope, click chips directly via `page.getByText(title).first()`. Verified 3× green on chromium (22s avg) and once on webkit (58s) locally. The calendar-chip rendering is the new entry point for activities — the deleted upcoming list answered the same "what's next?" question and the chip-with-time treatment now serves that purpose.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-05-31 | `planner.spec.ts:140/205/311` + `cross-entity.spec.ts:458` — all "Doctor Visit" / "Weekly Lesson" / "Reschedule Test" / "Guitar Lesson" click timeouts on **both chromium AND webkit**. Webkit also kept the recurring `selectAccountType` + `/transactions` navigation flakes documented 2026-05-05 → 05-16. | (a) + (c) | **Real bug caught (a)**: the month-end variant of the test fixtures' "create activity for tomorrow" pattern. Each test built tomorrow via naive `new Date(); tomorrow.setDate(today.getDate() + 1)` — fine 30 days a month, broken on the last day. Today (May 31) → tomorrow = Jun 1 → activity persists but the planner's default month view (CalendarGrid fetches `monthActivities(year, month)`; padding cells from adjacent months don't render chips) still shows May → `page.getByText('Doctor Visit').first().click()` times out at 20s/60s waiting for a chip that's a month away. Hit by today's deploy (`/deploy-prod-auto` on commit `153094bd`); chromium-clean on the deploy follow-up. **Fix**: new `e2e/helpers/test-dates.ts` exporting `tomorrowOrTodayStr()` — returns tomorrow when in-month, clamps to today on month-end. Wired into `planner.spec.ts` (4 sites, including the `getTomorrowStr` helper + the "Reschedule" inline) and `cross-entity.spec.ts:458` (Loan & Activity Linking). Chromium 22/22 green after the fix. **Webkit-only residual (c)**: 3 hard-fails in the same shape as the long-running webkit-CI flakes — `page.goto('/transactions')` interrupted by `/nook` redirect (1×) + `AccountsPage.selectAccountType` subtype timeout at 30s (2× via `cross-entity` Loan/Activity setup) + `dismissActivityCreatedConfirm` 15s timeout (1×). All three patterns are documented multiple times above (05-05/09/13/16); not regressions from this deploy. The chromium-green signal carried the deploy; webkit retries are an acceptance gap that the existing flake-mitigation work is already tracking.                      |
| 2026-05-25 | `planner.spec.ts:198/233/263/288` — all 4 "Recurring: …" tests hard-fail on **both chromium AND webkit** (chromium 4 failed / webkit 4 failed + the long-running webkit `financial-data`/`cross-entity` contention flakes)                                                                                    | (a)       | **Real bug caught.** Each test creates a weekly activity starting _tomorrow_ then clicks its chip on the month calendar; the click timed out because the chip never rendered ("May 2026 — 0 activities"). DB export showed the activity persisted with `date: <tomorrow=Tue>` but `daysOfWeek: [1]` (**Mon = today's weekday**). Root cause in `ActivityModal.vue`: the `watch(date)` auto-fills `daysOfWeek` from the start date's weekday **only when the array is empty** — so it fills `[Mon]` on open (default date = today), then never updates when the user picks a different-weekday start date. The weekly recurrence then anchors to Mondays starting from a Tuesday; the first matching Monday (Jun 1) falls outside May → zero May occurrences → invisible on the calendar. Latent since `28aedba` (daysOfWeek added); the `ea66dd4` redesign **exposed** it because the calendar month view is now the only entry point — the old upcoming-list looked 3 months ahead so it still showed the (wrongly-dated) occurrence and the tests passed. **Fix**: the watcher now follows the start date's weekday while the selection is "untouched" (empty, or still the previous start date's single weekday), preserving genuine multi-day / hand-edited picks. Verified: all 5 planner tests green on chromium (21.7s) and webkit (1.2m). The webkit `financial-data`/`cross-entity` CI failures are the documented webkit-CI navigation/`selectAccountType` flakes (see 05-05 → 05-16) — chromium-green, pass on `gh run rerun --failed`; unrelated to this fix.                                                                                             |
| 2026-06-18 | `invite-join.spec.ts:118` "Drive join failure triggers Picker fallback" hard-fail on **both chromium AND webkit** across several pushes (red since the 2026-06-17 wrap-up commit, before the lists/badge work) — `getByRole('button', { name: /select file from drive/i })` not found at 15s.                 | (b)       | **Stale copy-dependent selector.** The awaiting-auth Picker CTA was renamed from "Select file from Drive" to **"Open Your Family File"** (`join.pickerPrompt.button`) in the 2026-06-14 join-flow copy pass ("reframes the Drive step as the clear final step"). The test's regex was never updated, so it timed out waiting for a button that no longer exists — pure test drift, not a product regression (the button renders correctly via `BaseButton` in `JoinPodView.vue:384`, and the `orManual` "load from device" fallback is genuinely absent for cloud joiners, so the line-166 negative assert still holds). Fix: updated the selector regex to `/open your family file/i` with a comment tying it to the i18n key. Verified green on chromium locally (5.4s). The chromium `cross-entity` institution timeouts in the same run passed on retry (documented webkit/CI contention flake class) — this was the only hard failure on both browsers. Surfaced during a prod deploy where E2E (an ungated separate workflow) was red while Main CI + Security were green. Lesson: these invite-join selectors are copy-based throughout the spec; if this recurs, migrate the CTA assertions to `data-testid`.                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-06-03 | webkit-only: `financial-data.spec.ts:37` (`page.goto('/transactions')` interrupted by `/nook`) + `cross-entity.spec.ts:420` (`dismissActivityCreatedConfirm` 15s visibility timeout) — on consent-refresh commit `2930df5`                                                                                    | (c)       | Chromium (the deploy gate) green; webkit hard-failed the two documented contention flakes. **Not a regression**: the commit's only adjacent change is `ActivityModal.vue`'s `maybeAttachSourcePhoto`, which is fully gated (`if (!photo \|\| firstMissingFieldKey.value !== null) return`) and a complete no-op for any non-photo create — confirmed by reading the diff. `gh run rerun --failed` passed clean (webkit + chromium ✓ on the same SHA), confirming pure flake. **Structural harden**: the `page.goto` interrupt class (recurring since 2026-05-11; the `commit`-only harden didn't fully absorb it — the interrupt can beat the response commit under contention) now goes through a new `gotoRoute(page, path)` helper in `helpers/navigation.ts` that retries the goto on `interrupted by another navigation` / `Frame load interrupted`, letting the outgoing page's redirect settle (`waitForLoadState('domcontentloaded')`) between attempts. `gotoRoot` is now a thin wrapper over it, and the ~10 copy-pasted `page.goto(path, { waitUntil: 'commit' })` + comment sites across 4 page-objects (Transactions/Accounts/Assets/Dashboard) and 3 specs (cross-entity ×3, planner, trusted-device) were consolidated through it (DRY). Non-interrupt (chromium) path is byte-for-byte identical behaviour. Verified all migrated specs green on chromium single-worker (financial-data + planner 7/7, cross-entity Data-integrity, trusted-device). The `cross-entity` 15s confirm-modal timeout has no clean code fix (pure contention) — rerun remains its resolution.                                                                             |

---

## Invite Wizard (2-step) — manual cross-browser test matrix

Recorded as part of `docs/plans/2026-04-26-invite-wizard.md`. Run after the wizard ships and before closing the change. Two entry points × two providers × happy + error paths.

| Entry point    | Provider     | Path            | Expected                                                                                         | Verdict (greg) |
| -------------- | ------------ | --------------- | ------------------------------------------------------------------------------------------------ | -------------- |
| Invite Beanie  | google_drive | happy           | Step 1 → Drive shares with email → Step 2 with QR + channel grid                                 | _pending_      |
| Invite Beanie  | google_drive | network offline | Step 1 submit fails → inline error chip with Try Again → re-submit on reconnect succeeds         | _pending_      |
| Invite Beanie  | google_drive | typo @gmial.com | Drive 403 → inline "double-check it's a Gmail/Workspace address" with edit-email recovery        | _pending_      |
| Invite Beanie  | local        | happy           | Step 1 CTA reads "Confirm \<email\>" → no Drive call → Step 2 with QR + reminder about .beanpod  | _pending_      |
| Per-bean share | google_drive | happy           | Wizard opens prefilled, email pulses; user confirms → Drive share → Step 2                       | _pending_      |
| Per-bean share | google_drive | back to Step 1  | Step 2 → "Use a Different Email" → Step 1 (email retained, checkbox unticked) → confirm → Step 2 | _pending_      |
| iPhone Safari  | google_drive | happy           | Full-screen modal renders; QR scannable from another phone                                       | _pending_      |
| Android Chrome | google_drive | happy           | Same                                                                                             | _pending_      |
| Installed PWA  | google_drive | happy           | Same                                                                                             | _pending_      |

## Joiner onboarding hardening (#185) — manual cross-browser test matrix

Recorded as part of the rev-3 plan (`docs/plans/2026-04-26-joiner-onboarding-hardening-rev3.md`). Run after the joiner-flow refactor ships and before closing #185. Use a real invite URL generated from the Meet The Beans share modal (must include `t=`, `f=`/`fam=`, `p=google_drive`, `fileId=`, and ideally `hint=`). Each cell records a verdict:

- `works` — happy path completes through to pick-member + set-password.
- `works with caveat` — completes but with a noticeable hiccup (note the caveat).
- `needs different device` — the floor; user must complete first-load on another device, then iPhone works thereafter.
- `broken — bug filed` — link the issue.

### Phase 0.5 + Phase 4 verdict matrix

| Device / Browser             | Fresh first-load (cookies cleared) | Returning user (warm cookies) | Picker iframe error path | Wrong-account picked |
| ---------------------------- | ---------------------------------- | ----------------------------- | ------------------------ | -------------------- |
| iOS Safari (latest, regular) | _pending greg's iPhone_            | _pending_                     | _pending_                | _pending_            |
| iOS Safari (standalone PWA)  | _pending_                          | _pending_                     | _pending_                | _pending_            |
| iOS Chrome (regular)         | _pending_                          | _pending_                     | _pending_                | _pending_            |
| Android Chrome               | _pending_                          | _pending_                     | _pending_                | _pending_            |
| Android Chrome (standalone)  | _pending_                          | _pending_                     | _pending_                | _pending_            |
| Desktop Chrome               | _pending_                          | _pending_                     | _pending_                | _pending_            |
| Desktop Firefox              | _pending_                          | _pending_                     | _pending_                | _pending_            |
| Desktop Safari               | _pending_                          | _pending_                     | _pending_                | _pending_            |

Verdict notes go below the row. Phase 11 (iOS Storage Access workaround) only runs if iOS Safari standalone PWA still fails after Phase 0.5; document the result here whether kept or removed.

### Diagnostic snapshot pre-Phase-0.5 (greg, 2026-04-26)

Real invite URL on iPhone iOS 26.4.2 regular Safari, before the Phase 0.5 fix:

- Picker CTA tap → Google consent → 2FA → "Allow Google access to your necessary cookies" → user allows → "The API developer key is invalid." inside the popup/tab.
- iOS Chrome: same failure.
- "Block All Cookies" = OFF; Cross-Site Tracking Prevention = ON (default); Storage Access API = ON.
- The same flow had worked for greg's wife on iPhone 17 a few weeks prior, when iOS regular Safari was using popup-based OAuth (before the over-correction in commit `a8f1a24`).

Phase 0.5 reverts `shouldUseRedirectAuth()` so iOS regular Safari uses popup again. The matrix above captures whether that fix actually resolves the cookie/API-key chain on each combo.
