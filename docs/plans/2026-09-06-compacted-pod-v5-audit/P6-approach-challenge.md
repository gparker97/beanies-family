# P6 — Approach challenge: is a version bump the right mechanism?

> Date: 2026-09-06
> Deployed build audited: `c3a6be98` (0.16). Current `main`: `72d02eb4`.
> Status: INCOMPLETE — being written incrementally. The last section written is the
> one before the marker `<!-- END-OF-WRITTEN -->`.

## Verdict

_(written last; see the end of this file if the marker below has moved past section 7)_

## 1. Alternatives: where each one fails on the deployed build, and whether that point is safe

The deployed reader, VERIFIED at `c3a6be98`:

- `src/services/sync/fileSync.ts:59-84` `parseBeanpodV4`: `JSON.parse` (throws plain `Error('Invalid JSON in beanpod file')`), object check, then **`if (obj.version !== '4.0') throw new Error('Unsupported beanpod version: …')` at :73-74, BEFORE** the `familyId` / `familyName` / `keyId` / `encryptedPayload` / `wrappedKeys` checks at :76-82. Nothing is decrypted, nothing is unwrapped, nothing is posted to the worker.
- `fileSync.ts:92-100` `detectFileVersion`: returns `'4.0'` or `null`; every other value, and unparseable JSON, is `null`.
- Every Drive/local read site guards with one or both of those BEFORE any dispatch: `syncStore.ts:970-976` (`loadFromFile`: `detectFileVersion` → `error.value = 'Unsupported file version: …'; return {success:false}`), `syncStore.ts:3554-3560` (`loadFromGoogleDrive`, same shape, `reason:'error'`), `syncService.ts:1192` (`fetchAndMergeRemote`: `parseBeanpodV4` before `learnRemoteMarker` :1196, before `setEnvelope` :1211, before `mergeRemoteEnvelope` :1201), `syncService.ts:1491` (`loadAndParseV4`, inside a try whose catch only does `updateState({lastError})`), `syncService.ts:1541/1593/1633` (the three picker/drop entry points: `detectFileVersion` then `lastError: 'Unsupported file version: …'`).
- The deployed build reads NO other envelope field before the version check. `writerVersion` is written (`fileSync.ts:49`, `:214`) and never read anywhere in `src/` (VERIFIED by `git grep writerVersion c3a6be98 -- src`: only the two writers and the type).

| Rank  | Mechanism                                                                                       | Where it fails on `c3a6be98`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Safe?                                                                                                                                                                                                                                                                                                                                             |
| ----- | ----------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **1** | **(a) `version: '5.0'`**                                                                        | `parseBeanpodV4` :73 / `detectFileVersion` :96 — at PARSE, before decrypt, before the worker, before the marker or key-dict adoption                                                                                                                                                                                                                                                                                                                                                                              | **YES.** It is the one failure the deployed build was _written_ to handle: every read site has an explicit `Unsupported file version` branch that returns a result rather than throwing (see §2). Same point as "the file is not a beanpod at all".                                                                                               |
| 1=    | (g-i) any other structural parse break (non-object JSON, non-JSON bytes, missing `wrappedKeys`) | same lines (:66, :62, :81)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Same safety, strictly worse: undesigned, and `detectFileVersion` cannot tell it apart from garbage. No reason to prefer over (a).                                                                                                                                                                                                                 |
| —     | **(b) new required field / `minReaderVersion`**                                                 | **Nowhere.** `parseBeanpodV4` checks six named keys and ignores the rest; `writerVersion` proves the pattern (written since #44, never read). VERIFIED.                                                                                                                                                                                                                                                                                                                                                           | Does not protect the deployed build AT ALL. It is still the right thing to ADD now, honoured by the NEW build, so the _next_ format change is a graceful "update beanies to open this" instead of a hard bump (see §7).                                                                                                                           |
| 3     | (c) new `keyId` / re-wrapped keys                                                               | Password path: `tryUnwrapFamilyKey` → `decryptPendingFile` catch `syncStore.ts:1358-1368` maps `'Incorrect password'` to the re-prompt (VERIFIED). **Cached-key path: does not fail at all** — `decryptPendingFileWithKey` and the trusted-device `loadFromFile` merge use the raw family key, which a re-wrap does not change; only a family-key ROTATION would, and that invalidates every cached key on every NEW device too (`LoadPodView.vue:167-188` `tryAutoDecrypt` clears the cache key on any failure). | **NO.** Either useless (re-wrap) or a fleet-wide re-prove plus an indistinguishable-from-typo password loop on stale devices (rotation).                                                                                                                                                                                                          |
| 4     | (d) payload framing that decrypts but does not materialise                                      | `decryptToDoc` → `CorruptPayloadError` (`docOps.ts:228/238`). Cold path: `completeAutoLoad` `syncStore.ts:3729-3735` → `kind:'corrupted'` → fatal "contact support" overlay. Cache path: `applyAndProject.ts:487-499` `initAndLoadCache` catch → **`cache.clearCache(id)`** — but only for a corrupt CACHE, which a remote v5 file cannot produce. Poll path: caught as an ordinary error.                                                                                                                        | **NO** — it fails at the last possible point, after a full download and decrypt, into the code paths that treat the file as damaged; and it lies to the user ("your file is corrupt").                                                                                                                                                            |
| 5     | (e) publish the compacted pod to a NEW Drive file                                               | Nowhere on the stale device: it keeps reading and writing the OLD fileId (the provider is persisted by fileId, `fileHandleStore`), so it never sees the new file at all. New-build devices must be re-pointed via the registry pointer and `CANONICAL_MISMATCH` (`ADR-033 §5-6`).                                                                                                                                                                                                                                 | **NO.** ADR-033's reason applies with full force: this is exactly a fork — two live files receiving edits — and it makes every current device show the "working on a copy" banner until a human re-picks. The stale device's edits are lost either way (as in (a)), but here they are lost into a file nobody reads AND the family has two homes. |
| 6     | (f) a lineage marker the old build chokes on                                                    | Cannot: `git grep -n 'lineage' c3a6be98 -- src` returns nothing (VERIFIED, see §6). `podLineage` is ordinary document data to it.                                                                                                                                                                                                                                                                                                                                                                                 | Not a mechanism.                                                                                                                                                                                                                                                                                                                                  |
| 7     | (g-ii) change `familyId` on compaction                                                          | After parse: `syncService.ts:1494-1501` and `syncStore.loadFromFile` skip on familyId mismatch. `fetchAndMergeRemote` :1201 does NOT check (it passes `remoteEnvelope.familyId` straight to the worker) — INFERRED that the worker's own `currentFamilyId` check would refuse; not traced.                                                                                                                                                                                                                        | **NO.** Breaks the registry row, invite links, the key cache (`cacheFamilyKey(…, familyId)`), the IDB database name, and every `activeFamilyId` comparison. Enormous blast radius for a failure point later than (a).                                                                                                                             |

**Conclusion for Q1: (a) is the only candidate that fails before any destructive dispatch on the deployed build, and it is the only one whose failure mode the deployed build explicitly anticipates. Nothing better exists, because there is no earlier point than the version literal to fail at.** The one thing (a) cannot do — protect a stale device that opens from its own cache and later SAVES — is not fixable by any envelope-side mechanism, since the stale device never reads the envelope before writing (P2/P3 traces this).

One design decision the plan must make explicit (it is not in the README): **is the version a property of the DOCUMENT (stamped → `'5.0'`) or of the WRITER (a new build writes `'5.0'` always)?** OPINION: it must be the document. The writer rule would break every stale device in every family at deploy time, before any compaction, including the safety copy and manual exports of never-compacted pods; it turns "families that compact accept a stale-device cost" into "every family pays it on day one". The document rule confines the cost to the operation that needs it, keeps the safety copy at `'4.0'` (a full-history, unstamped document that a stale device can legitimately read — with one caveat in §6.5), and makes "two compactions → still 5.0" true by construction (§6.1).

<!-- END-OF-WRITTEN -->
