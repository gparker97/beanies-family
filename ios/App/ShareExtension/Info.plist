<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Share Extension (#64).

  NSExtensionActivationRule is what decides whether beanies APPEARS in another app's share
  sheet, so it is the security boundary as much as the UX one: images, PDFs, web URLs and
  selected TEXT, each with a bounded count. `MaxCount` of 5 mirrors MAX_EXTRACT_PAGES on the
  JS side — the app reads at most five pages, so offering to accept twenty would promise
  something it will not do.

  Text (#83) is `NSExtensionActivationSupportsText`, a BOOLEAN — not a `…WithMaxCount`
  integer like the four keys below it. There is no count to bound: a text share is one
  selection. Its SIZE is bounded on the JS side instead (MAX_SHARE_TEXT_BYTES, decided from
  `File.size` before decoding), because the extension's `.plainText` branch already writes a
  shared string into the app group as a .txt, which is the same path a shared web URL takes.

  NSExtensionPrincipalClass is deliberately the plain view controller rather than
  SLComposeServiceViewController: there is no compose UI, the extension writes to the app
  group and exits immediately. See ShareViewController.swift.
-->
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>beanies.family</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>XPC!</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionAttributes</key>
		<dict>
			<key>NSExtensionActivationRule</key>
			<dict>
				<key>NSExtensionActivationSupportsImageWithMaxCount</key>
				<integer>5</integer>
				<key>NSExtensionActivationSupportsFileWithMaxCount</key>
				<integer>5</integer>
				<key>NSExtensionActivationSupportsAttachmentsWithMaxCount</key>
				<integer>5</integer>
				<!-- Links (#64 links). Safari and most apps share a page as a web URL; the
				     extension writes it into the app group as a .txt item, and the
				     orchestrator normalises that back to text for every platform. -->
				<key>NSExtensionActivationSupportsWebURLWithMaxCount</key>
				<integer>1</integer>
				<!-- Selected text (#83). The ONLY iOS change the feature needs: the
				     extension's .plainText branch already exists and writes the string as
				     a .txt, which ShareIntentPlugin maps to text/plain and the
				     orchestrator picks up on its textFromFile path. Boolean, not a count. -->
				<key>NSExtensionActivationSupportsText</key>
				<true/>
			</dict>
		</dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.share-services</string>
		<key>NSExtensionPrincipalClass</key>
		<string>$(PRODUCT_MODULE_NAME).ShareViewController</string>
	</dict>
</dict>
</plist>
