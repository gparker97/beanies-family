<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Share Extension (#64).

  NSExtensionActivationRule is what decides whether beanies APPEARS in another app's share
  sheet, so it is the security boundary as much as the UX one: only images and PDFs, and a
  bounded count. `MaxCount` of 5 mirrors MAX_EXTRACT_PAGES on the JS side — the app reads at
  most five pages, so offering to accept twenty would promise something it will not do.

  NSExtensionPrincipalClass is deliberately the plain view controller rather than
  SLComposeServiceViewController: there is no compose UI, the extension writes to the app
  group and exits immediately. See ShareViewController.swift.
-->
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>beanies.family</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>XPC!</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionAttributes</key>
		<dict>
			<key>NSExtensionActivationRule</key>
			<dict>
				<key>NSExtensionActivationSupportsImageWithMaxCount</key>
				<integer>5</integer>
				<key>NSExtensionActivationSupportsFileWithMaxCount</key>
				<integer>5</integer>
				<key>NSExtensionActivationSupportsAttachmentsWithMaxCount</key>
				<integer>5</integer>
				<!-- Links (#64 links). Safari and most apps share a page as a web URL; the
				     extension writes it into the app group as a .txt item, and the
				     orchestrator normalises that back to text for every platform. -->
				<key>NSExtensionActivationSupportsWebURLWithMaxCount</key>
				<integer>1</integer>
			</dict>
		</dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.share-services</string>
		<key>NSExtensionPrincipalClass</key>
		<string>$(PRODUCT_MODULE_NAME).ShareViewController</string>
	</dict>
</dict>
</plist>
